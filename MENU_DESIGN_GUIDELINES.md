# מדריך שיחזור ותחזוקה: תפריט השלטים (Body-Line Visual DNA)

קובץ זה נועד להבטיח את שרידות העיצוב הייחודי של תפריט השלטים במקרה של עדכוני קוד, שינויי תשתית או שחזור המערכת. התפריט נחשב לנכס ויזואלי מרכזי באתר.

## 1. רכיבי הליבה (Core Components)
התפריט מורכב מהרכיבים הבאים בתיקיית `src/components/`:
- **WoodSignLink.tsx**: הרכיב האחראי על השלט הבודד. כולל אפקטים של Glassmorphism, טקסטורת עץ, וסיבוב רנדומלי.
- **AngryBird.tsx**: הציפור האינטראקטיבית שיושבת על השלט הראשון.
- **SignPost.tsx**: עמוד העץ המרכזי (SVG).
- **RespectLocalsSign.tsx**: שלט ה-"Respect the Locals" העליון.

## 2. חוקי ה-DNA הויזואלי (Visual DNA Rules)
כדי לשמור על המראה היוקרתי, יש להקפיד על:
- **Glassmorphism**: שקיפות גבוהה, `backdrop-filter: blur(15px)`, וגבול של `1px solid white/20`.
- **סיבוב רנדומלי**: כל שלט חייב לקבל זווית סיבוב קלה (בין -3 ל-3 מעלות) כדי ליצור מראה טבעי של עבודת יד.
- **אפקט Swing**: ב-Hover, השלט צריך להתנדנד קלות סביב ה-"מסמר" המרכזי.

## 3. פרופורציות ומיקומים קריטיים
- **גודל שלטים**: רוחב מקסימלי של `256px` בדף הראשי ו-`280px` בתפריט הצד (Drawer).
- **הציפור (AngryBird)**:
  - בדף השלטים: `scale-[0.935]`, מיקום `-top-21`.
  - בתפריט הצד: `scale-[0.6375]`, מיקום `-top-17`.
  - הציפור חייבת לשבת בדיוק על קו המסגרת העליון של השלט הראשון.
- **Overflow**: חובה להשתמש ב-`overflow-visible` על מיכלי השלטים כדי שהציפור והאפקטים לא ייחתכו.

## 4. מדיניות עדכון קוד (Strict Append Policy)
- **לעולם אין למחוק או לדרוס פונקציות קיימות בתפריט.**
- כל שינוי חייב להיות אדיטיבי (Additive only).
- אם מוסיפים שלט חדש, יש להשתמש ב-`WoodSignLink` ולוודא שהוא מקבל `index` נכון לצורך חישוב הדיליי באנימציה.

## 5. תלות טכנולוגית (Dependencies)
- **Tailwind CSS**: לכל העיצוב וה-Glassmorphism.
- **Motion (framer-motion)**: לכל האנימציות והתנודות.
- **Lucide React**: לאייקונים בתוך השלטים.

---
*נכתב על ידי ה-Chief Architect של Body-Line לשמירה על שלמות העיצוב.*
